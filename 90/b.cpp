#include <iostream>
#include <algorithm>
#include <string>
using namespace std;

int main()
{
    int m = 0, s = 0, sum = 0, flag = 0;
    string min, max, str;
    cin >> m >> s;

    for (int i = 0; i < m; i++)
    {
        for (int j = 0; j < 10 ;j++)
        {
            if (i == 0 && j == 0) continue;
            {
                str +=
            }
        }
    }

    return 0;
}